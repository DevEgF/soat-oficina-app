# Retomada após a sessão de 07/09/2026

Este checkpoint complementa e atualiza o handoff de 31/08. O usuário pediu
uma nova branch, salvar o trabalho e continuar no dia seguinte. Não iniciar
deploy nem trabalho adicional durante o encerramento.

## Branches e diretórios

Branch criada nos três repositórios trabalhados: `checkpoint/2026-09-07-retomada`.

| Repositório | Diretório local | Commit de implementação preservado |
|---|---|---|
| app / planejamento | `D:/dev/Backend/soat-tech-challenge-oficina` | `b7abedc` |
| infra-k8s | `.tmp/infra-k8s-resume` neste workspace | `682a28e` |
| infra-db | `.tmp/infra-db-resume` neste workspace | `c0af95e` |

Os dois diretórios `.tmp/*-resume` são worktrees Git vinculados aos repositórios
irmãos em `D:/dev/Backend`. Preservá-los. As branches anteriores continuam
existindo. As branches de checkpoint são locais; não houve push neste encerramento.

O repositório irmão `soat-oficina-auth` está limpo no commit `9906d87`; contém
o esqueleto documental, sem implementação da autenticação da Fase 3.

## Trabalho concluído

- Toolchain, topologia GitHub e contratos entre repositórios já têm commits.
- Fundação Kubernetes e infraestrutura PostgreSQL implementadas localmente.
- `682a28e`: permissões de lifecycle do banco, logs/alarmes, leitura do state
  da fundação, KMS limitado pelas tags Project/Component e pelo serviço,
  criação do segredo gerenciado sem permissão de ler seu valor.
- `c0af95e`: segredo gerenciado do RDS criptografado pela chave do banco;
  aplicação pode descriptografar somente o segredo exato via Secrets Manager;
  workflow de destroy copia o snapshot final para `alias/aws/rds` antes de
  remover a chave Terraform e exige a cópia disponível antes de desproteger o banco.
- Documentação e testes acompanham as alterações.

## Validações executadas

- infra-db: `terraform validate`, 4 testes Terraform, fmt, TFLint e os dois
  scripts PowerShell de docs/workflows passaram.
- infra-k8s: `terraform validate`, 7 testes Terraform, fmt, TFLint e os cinco
  scripts PowerShell de contratos/policies/workflows/custo passaram.
- Checkov final: infra-db 53 checks passaram, 0 falharam, 3 suprimidos;
  infra-k8s 136 passaram, 0 falharam, 30 suprimidos.
- Limitação: o Checkov da fundação não analisou internamente os módulos externos
  EKS/VPC; avisou que seria necessário `--download-external-modules`.
- `git diff --check` passou; os worktrees de infraestrutura ficaram limpos
  depois dos commits.
- No Windows, usar `terraform test` para a suíte completa; o filtro com `/`
  não encontrou os arquivos e reportou zero testes. Zero testes não é sucesso do gate.
- O atalho `checkov.cmd` escolhe o Python errado. Invocação local funcional:

```powershell
& 'C:/Users/egito/AppData/Roaming/uv/tools/checkov/Scripts/python.exe' 'C:/Users/egito/.local/bin/checkov' -d . --framework terraform --quiet
```

## Próxima sessão

1. Ler AGENTS.md, este checkpoint e os planos existentes; continuar nas branches
   e worktrees acima, sem redesenhar a arquitetura.
2. Conferir estado remoto de PRs/Actions e recursos AWS antes de alterar algo.
   Há evidências locais de applies anteriores da fundação, mas esta retomada
   não revalidou o estado vivo da AWS e não executou apply/destroy.
3. Integrar as correções de infra-k8s antes do deploy de infra-db. Gerar novos
   planos: os `.tfplan` antigos antecedem os commits atuais e não devem ser aplicados.
4. Antes de apply, confirmar identidade, região, plano e custo conforme o roteiro.
   O script local da fundação estimou US$ 10,33/40h e US$ 188,55/730h nesta sessão;
   verificar o custo real acumulado e a janela ativa. Teto aprovado: US$ 20.
5. Concluir deploy e aceite do banco; depois implementar Lambda CPF/JWT e
   authorizer/API Gateway, adaptação da aplicação (ownership e ambientes),
   Helm/migrations, CI/CD e promoção hml/prod.
6. Validar a jornada completa, observabilidade e rollback; finalizar documentação,
   evidências e vídeo. Prazo registrado no plano: 15/09/2026.

## Autorizações e preservação

- O usuário autorizou expressamente substituir a skill indisponível
  `aws-secrets-manager` por `creating-secrets-using-best-practices`, obtida
  pelo AWS MCP. Não pedir essa autorização novamente nesta retomada.
- Continuam proibidas leituras de valores de Secrets Manager no contexto do agente.
- Não usar subagentes sem solicitação explícita do usuário.
- Preservar os itens preexistentes não rastreados: `$null`, `.claude-flow/`,
  `.superpowers/`, `.tmp/`, `AGENTS.md` e `docs/rfcs/`. Eles não foram incluídos
  no commit de checkpoint. `.tmp/` contém worktrees e planos binários Terraform;
  não adicionar recursivamente ao repositório app.
- Nenhum segredo foi lido ou colocado nos commits desta retomada.

## Avaliação de progresso

A base de infraestrutura está bem adiantada, mas ainda há uma parte substancial
da entrega: autenticação serverless, integração da aplicação, deploy nos dois
ambientes e evidências. Testes locais verdes não comprovam o aceite em AWS.
