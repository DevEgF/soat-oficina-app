# OCI private deployment checkpoint

Verified on 2026-09-12 America/Sao_Paulo (2026-09-13 UTC).

- K3s node oficina-oci Ready; hml/prod API and auth Ready with no restarts.
- Both oficina Helm releases deployed at revision 1, identical ARM64 digests.
- Flyway completed seven migrations in each Neon schema over verify-full TLS.
- New Relic nri-bundle 8.0.24 revision 3, all five pods Ready without restarts.
- VM snapshot: 117m CPU, 2392Mi memory (21%); SELinux Enforcing.
- Secrets remain in root-owned 0700 directories and 0600 files under /etc/oficina.
- No AWS files changed and no AWS operations performed.

## Runtime findings fixed

Hibernate default_schema is supplied using SPRING_APPLICATION_JSON: environment
variable underscore conversion did not preserve the map property's exact key.
Fluent Bit uses a dedicated offset directory and a collector-only spc_t SELinux
context to read host logs. It drops capabilities and disallows privilege escalation;
the application namespaces retain restricted PodSecurity.

## Smoke evidence

`sudo python3 /opt/oficina/oci/smoke-vm.py hml` passed health, staff login, database
read, malformed CPF rejection, synthetic customer creation and CPF authentication,
customer/admin denial and customer JWT acceptance on a missing-OS lookup (404).
The synthetic customer was removed (204). This is not a complete ownership/OS test.

`sudo python3 /opt/oficina/oci/smoke-vm.py prod` passed health, staff login, database
read, malformed CPF rejection and rejection of cross-environment staff JWTs in
both directions (401). It did not create business records in production.

## New Relic UI evidence

Account 8439293, US. Queries run using the authenticated Chrome UI:

- K8sNodeSample for clusterName oficina-oci: 28 samples in a 30-minute window.
- Log for cluster_name oficina-oci: 52 records in a 30-minute window.
- Span for appName oficina-hml: 456 spans in a 30-minute window.
- Transaction FACET appName: oficina-hml 75, oficina-prod 19 in a later 30-minute window.

Counts are snapshots, not rates or total historical volume. Key values and
database passwords are intentionally absent from this record.

## Still pending

Public HTTPS/OCI ingress rules, complete OS journey and ownership smoke, OCI
rollback exercise, registry publication and automated CI/CD, frontend deployment,
business metrics/dashboard/alerts and final acceptance video evidence. Containerized
auth does not satisfy the serverless criterion; the preserved Lambda implementation
remains its reference. Neon currently shares an owner role across the two schemas;
JWT isolation does not imply database privilege isolation.
