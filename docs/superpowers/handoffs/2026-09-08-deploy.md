# Deploy da fundação e banco - 08/09/2026

O usuário autorizou continuar com apply da fundação e implantação do banco.

## Fundação

A infraestrutura está ativa em us-east-1. Um nó EKS 1.35 está Ready e todos os pods de sistema estão Running. Os sete add-ons retornaram ACTIVE, sem health issues. O acesso de oficina-admin foi preservado.

Foram configuradas as variáveis não secretas AWS_REGION e AWS_ROLE_ARN nos oito environments; TF_STATE_BUCKET nos repositórios de infraestrutura, ALERT_EMAIL na fundação e EKS_CLUSTER_NAME/ECR_REPOSITORY_URL no app.

PRs integrados na fundação: #1 a #6. PRs integrados no banco: #1 e #2. Todos os merges preservaram os checks obrigatórios.

Correções verificadas durante os deploys reais:

- Subjects OIDC com IDs imutáveis do GitHub, mantendo StringEquals e ambiente exato.
- CloudWatch: operações de leitura e tagging necessárias ao Terraform.
- Administrador EKS e KMS fixo, independente do executor local/OIDC.
- Service-linked role RDS criada explicitamente antes do subnet group.
- PassRole limitado à role de monitoramento com iam:PassedToService=rds.amazonaws.com.
- DescribeKey da chave padrão do Secrets Manager limitado por kms:ResourceAliases e kms:ViaService. Nenhum decrypt nem leitura de valores concedido para essa descoberta.

A pipeline da fundação já concluiu com plano sem diferenças e apply 0/0/0:
https://github.com/DevEgF/soat-oficina-infra-k8s/actions/runs/34288377074

## Banco

A instância soat-oficina-db iniciou criação em 08/09 às 22:56 UTC. Parâmetros confirmados via DescribeDBInstances: PostgreSQL 16.13, db.t4g.micro, Single-AZ, 20 GB gp3, criptografado, sem acesso público, proteção contra exclusão e backup de um dia.

Execução de deploy acompanhada:
https://github.com/DevEgF/soat-oficina-infra-db/actions/runs/34287002625

Resultado final: RDS AVAILABLE, segredo gerenciado ACTIVE e pipeline SUCCESS. Os três alarmes existem, com ações habilitadas; inicialmente INSUFFICIENT_DATA enquanto coletam métricas. TLS rds.force_ssl=1 e ingress somente EKS/Lambda na porta 5432 confirmados. Evidência: docs/delivery/evidence/2026-09-08-infrastructure.json.

## Continuidade e limites

- Worktree fundação: .tmp/infra-k8s-resume, branch fix/rds-managed-key-discovery, commit ee73843 (PR #6 integrado).
- Worktree banco: .tmp/infra-db-resume, branch fix/rds-service-linked-role, commit ac545ab.
- Planos antigos não podem ser reutilizados: o state mudou durante os applies. Gerar novo plano para qualquer próximo apply.
- Os logs e planos locais não foram incluídos no Git. Preservar demais arquivos não rastreados.
- Último Cost Explorer: US$ 3,9411873256 no mês antes de créditos, ainda Estimated; orçamento aprovado US$ 20.
- Fundação: 7 testes Terraform passaram; banco: 4 passaram. CI protegido e scans passaram nos PRs integrados.
- A análise Checkov dos módulos externos continua sendo uma limitação conhecida.
- Não houve leitura de valores Secrets Manager. A validação funcional de conexão da aplicação com o banco ainda depende de migrations e workloads da próxima etapa.

Próxima etapa: implementar aplicação/auth/Helm e migrations conforme os planos existentes. O banco está provisionado, mas os schemas e a jornada funcional ainda dependem dos workloads. Não tratar este deploy como conclusão da Fase 3.
