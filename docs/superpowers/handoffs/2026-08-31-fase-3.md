# Retomada da Fase 3 em 01/09/2026

Este arquivo é o ponto único de retomada para Claude ou Codex. Ele registra o estado confirmado em 31/08/2026 e deve ser lido antes de qualquer instalação, alteração remota ou criação de recurso AWS.

## Objetivo e disponibilidade

- Entregar a Fase 3 do Tech Challenge até 15/09/2026.
- Trabalho individual, aproximadamente duas horas por dia.
- Documento-fonte: `C:\Users\egito\Downloads\13SOAT - Fase 3 - Tech Challenge.pdf`.
- Workspace: `D:\dev\Backend\soat-tech-challenge-oficina`.

## Estado salvo

- Arquitetura e especificação aprovadas pelo usuário.
- Divisão em quatro repositórios, fluxo, observabilidade e planejamento aprovados.
- Especificação versionada no commit `fd21fc3`.
- Cinco planos e correção final da especificação versionados no commit `b1fa37c`.
- Nenhum recurso de infraestrutura da Fase 3 foi criado na AWS.
- Os quatro novos repositórios GitHub ainda não foram criados.
- Agent Toolkit for AWS e perfil `oficina-admin` foram configurados anteriormente para `us-east-1`; revalidar identidade e região antes de qualquer operação.
- Conta possui US$ 100 em créditos informados pelo usuário. O teto operacional aprovado para esta entrega é US$ 20, com estimativa antes de todo apply.

## Decisões congeladas

- Repositórios: `soat-oficina-infra-k8s`, `soat-oficina-infra-db`, `soat-oficina-auth` e `soat-oficina-app`.
- Fluxo: API Gateway HTTP API -> Lambda CPF/authorizer -> VPC Link -> NLB interno -> EKS -> RDS PostgreSQL.
- Um EKS e um RDS compartilhados para reduzir custo.
- Isolamento por namespaces e schemas `hml`/`prod`.
- NLB sem Ingress Controller: listener 8080 -> NodePort 30080 hml; listener 8081 -> NodePort 30081 prod.
- EKS 1.35, um node `t3.medium`, HPA de 1 a 3 pods.
- RDS PostgreSQL 16, `db.t4g.micro`, Single-AZ, 20 GB gp3.
- Sem NAT Gateway; RDS e Lambdas privados.
- Autenticação acadêmica por CPF ativo, JWT de 15 minutos, sem CPF nos claims/logs.
- CloudWatch + ADOT; métricas de negócio com baixa cardinalidade.
- GitHub Actions por OIDC, sem access keys.
- Pull Request obrigatório em `develop` e `main`, com zero aprovações humanas e checks obrigatórios.
- `develop` implanta hml; `main` promove para prod o digest imutável já validado.

## Documentos que devem ser lidos nesta ordem

1. `AGENTS.md`, preservando as regras locais do workspace.
2. `docs/superpowers/handoffs/2026-08-31-fase-3.md`.
3. `docs/superpowers/specs/2026-08-31-fase-3-aws-design.md`.
4. `docs/superpowers/plans/2026-08-31-fase-3-roadmap.md`.
5. O plano técnico do repositório que estiver sendo implementado.

Planos técnicos:

- `docs/superpowers/plans/2026-08-31-infra-k8s.md`
- `docs/superpowers/plans/2026-08-31-infra-db.md`
- `docs/superpowers/plans/2026-08-31-auth-serverless.md`
- `docs/superpowers/plans/2026-08-31-app-eks.md`

## Primeira atividade de amanhã

Executar somente a Task 0 do roteiro mestre:

1. criar primeiro o teste `scripts/test-prerequisites.ps1`;
2. confirmar as ferramentas ausentes;
3. solicitar aprovação antes de instalar pacotes;
4. instalar GitHub CLI, Terraform, TFLint, Checkov, Helm e PowerShell 7;
5. autenticar GitHub e revalidar AWS sem exibir credenciais;
6. registrar versões e fazer o commit definido no plano.

Auditoria de 31/08:

- presentes: Git, Docker, kubectl, Node 24, Java 21, Python e uv;
- ausentes no PATH: GitHub CLI, Terraform, TFLint, Checkov, Helm e PowerShell 7;
- `npm.ps1` é bloqueado pela política local; usar `npm.cmd` no Windows;
- Docker Desktop pode precisar estar aberto para `docker info` funcionar.

Não iniciar a criação dos repositórios da Task 1 enquanto o gate da Task 0 não estiver verde.

## Guardas obrigatórias

- Antes de qualquer AWS apply: mostrar ARN da identidade, região, plano Terraform e estimativa de custo.
- Não criar recursos em outra região.
- Não recuperar, imprimir ou salvar valores de Secrets Manager em sessão de agente.
- Não persistir CPF, JWT, senha ou access key.
- Não executar apply ou destroy em Pull Request.
- Não usar tags de imagem mutáveis.
- Não alterar os RFCs atuais até chegar à tarefa documental do plano app.
- Preservar os itens não rastreados já existentes: `.claude-flow/`, `.superpowers/`, `.tmp/`, `AGENTS.md` e `docs/rfcs/`.

## Prompt pronto para Claude ou Codex

Copiar e enviar exatamente este texto ao iniciar amanhã:

```text
Retome a Fase 3 deste workspace. Leia completamente AGENTS.md, docs/superpowers/handoffs/2026-08-31-fase-3.md, a especificação aprovada e o roteiro mestre indicados no handoff. Não redesenhe a arquitetura. Use o plano de execução existente e comece somente pela Task 0 do roteiro, seguindo TDD e fazendo o commit previsto. Não use subagentes sem minha solicitação explícita. Antes de instalar ferramentas, peça a aprovação necessária. Não crie recursos AWS nem repositórios GitHub nesta primeira tarefa. Preserve todos os arquivos não rastreados que não pertencem à tarefa e me informe o resultado do gate da Task 0.
```

## Gate para avançar à Task 1

- `scripts/test-prerequisites.ps1` passa integralmente.
- GitHub CLI está autenticado no usuário correto.
- AWS STS confirma o perfil `oficina-admin` e a região `us-east-1`.
- Terraform é 1.11 ou superior.
- Docker Engine responde.
- Nenhuma credencial aparece no log ou no repositório.
- O commit da Task 0 existe e a árvore de trabalho preserva os itens não relacionados.
