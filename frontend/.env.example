# Optional: API Gateway origin serving both /auth/token and /api/customer/**.
# Leave empty only when the frontend and gateway share the same origin.
VITE_CUSTOMER_API_BASE_URL=
# Staff API: leave empty for Vite's /api proxy to localhost:8080.
VITE_API_BASE_URL=
